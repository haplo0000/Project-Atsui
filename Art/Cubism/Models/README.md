# Cubism / Models

Live2D Cubism model project files (`.cmo3`).

Naming: `Atsui_v<MAJOR>.<MINOR>.<PATCH>.cmo3`

One primary model file per major rig version. Branch copies for experimental rigs should use suffix `_exp`.
